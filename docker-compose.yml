services:
  ktk-schedule:
    build: .
    image: ${KTK_SCHEDULE_IMAGE:-ktk-schedule:local}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/ktk-schedule.db
    volumes:
      - data:/app/data

volumes:
  data:
    name: ktk-schedule-data
