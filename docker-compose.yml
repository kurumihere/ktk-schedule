services:
  ktk-schedule:
    build: .
    container_name: ktk-schedule
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ktk_schedule_data:/app/data

volumes:
  ktk_schedule_data: