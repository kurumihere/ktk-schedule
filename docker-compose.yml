services:
  ktk-schedule:
    build: .
    image: ktk-schedule:${KTK_SCHEDULE_IMAGE_TAG:-local}
    container_name: ktk-schedule
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ktk_schedule_data:/app/data

volumes:
  ktk_schedule_data:
